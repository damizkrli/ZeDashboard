{% extends 'base.html.twig' %}

{% block title %}Mes Liens{% endblock %}

{% block body %}
    <div class="container">
        <h1>Mes liens professionnels</h1>

        <table class="table">
            <thead>
            <tr>
                <th>Nom</th>
                <th>URL</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% for pro in pros %}
                <tr>
                    <td>{{ pro.name }}</td>
                    <td>{{ pro.url }}</td>
                    <td>
                        <div class="d-flex align-items-center justify-content-start">
                            <form method="post" action="{{ path('app_delete_pro_link', {'id': pro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                                <a class="green-button" href="{{ path('app_edit_professional_link', {'id': pro.id}) }}"><i class="fa fa-scissors"></i></a>
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pro.id) }}">
                                <button class="red-button"><i class="fas fa-trash"></i></button>
                            </form>
                        </div>

                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">
                        <h2 class="fst-italic p-3">
                            <i class="fa fa-arrow-down"></i> Commencez par ajouter un lien
                        </h2>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a class="green-button" href="{{ path('app_add_pro_link') }}">Ajouter un lien</a>
        <a class="red-button" href="{{ path('app_link_index') }}">Retour</a>
    </div>
{% endblock %}
