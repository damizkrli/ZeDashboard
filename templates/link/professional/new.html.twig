{% extends 'base.html.twig' %}

{% block title %}Nouveau Lien{% endblock %}

{% block body %}

    <div class="container">
        <h1>Ajouter un lien professionnel</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a class="btn btn-sm btn-outline-success mb-5 rounded-0" href="{{ path('app_index_pro_link') }}">Retour</a>

    </div>

{% endblock %}
