{% extends 'base.html.twig' %}

{% block title %}New Platform{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter une nouvelle Plateforme</h1>

        {{ include('platform/_form.html.twig') }}

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a class="btn btn-sm btn-outline-dark" href="{{ path('app_platform_index') }}">Retour</a>
    </div>
{% endblock %}
