<form method="post" action="{{ path('app_apply_for_delete', {'id': apply_for.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apply_for.id) }}">
    <button class="btn">Delete</button>
</form>
